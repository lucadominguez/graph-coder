# Operator terminal helpers

Use `scripts/graph-coder-terminal-helper.ps1` or `.sh` to open Graph Coder status in Windows Terminal with `wt.exe`. Use `-DryRun` or `--dry-run` before launching. Komorebi is not used or required.
