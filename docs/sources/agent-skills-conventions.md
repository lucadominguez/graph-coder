# Agent Skills conventions used

Each portable skill has `SKILL.md` with concise YAML metadata containing `name` and `description`, followed by executable instructions, decision surfaces, evidence rules, schemas, and STOP/escalation boundaries.
